<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EstilosDemo.EstilosDemo"
             Title="EstilosDemo">

    <ContentPage.Resources>
        <Color x:Key="bgColor">#db3a34</Color>
        <Style TargetType="VerticalStackLayout">
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="Padding" Value="30" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="BackgroundColor" Value="{StaticResource bgColor}" />
        </Style>
        <Style TargetType="Button" x:Key="buttonBlue">
            <Setter Property="BackgroundColor" Value="Blue" />
        </Style>
    </ContentPage.Resources>
    <VerticalStackLayout>
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <!--Em este boton lo coge de manera Implicita que es la que viene por defecto-->
        <Button Text="Estilo Implicito" />
        <!--Em este boton lo coge de manera Explicita, le asignamos nosotros el estilo-->
        <Button Style="{StaticResource buttonBlue}" Text="Estilo explicito" />
        <!--Em este boton lo coge de manera General con un Diccionario-->
        <Button Style="{StaticResource botonRojo}" Text="BotonGeneral" />
    </VerticalStackLayout>
</ContentPage>