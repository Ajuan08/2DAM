import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'EjercicioCursos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
