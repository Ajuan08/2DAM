import { TuberiaMayusculasPipe } from './tuberia-mayusculas.pipe';

describe('TuberiaMayusculasPipe', () => {
  it('create an instance', () => {
    const pipe = new TuberiaMayusculasPipe();
    expect(pipe).toBeTruthy();
  });
});
